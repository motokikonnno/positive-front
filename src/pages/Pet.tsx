import type { NextPage } from "next";
import { Layout } from "../components/Layout";

const Pet: NextPage = () => {
  return (
    <Layout>
      <div>ペットページ</div>
    </Layout>
    );
};

export default Pet;